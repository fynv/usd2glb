#usda 1.0
(
    defaultPrim = "Ball"
    upAxis = "Z"
)

def "Ball" (
    assetInfo = {
        asset identifier = @assets/Ball/Ball.usd@
        string name = "Ball"
    }
    prepend references = @./Ball.geom.usd@</Ball>
)
{
}


