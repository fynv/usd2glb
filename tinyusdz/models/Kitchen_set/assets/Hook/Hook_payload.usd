#usda 1.0
(
    defaultPrim = "Hook"
    upAxis = "Z"
)

def "Hook" (
    assetInfo = {
        asset identifier = @assets/Hook/Hook.usd@
        string name = "Hook"
    }
    prepend references = @./Hook.geom.usd@</Hook>
)
{
}


